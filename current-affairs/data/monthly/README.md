# Monthly Current Affairs

Monthly views are generated automatically from the weekly files listed in `../manifest.json`.

Do not add duplicate monthly content here. Add each new week under:

`../weekly/YYYY/MM/YYYY-MM-DD.json`

Then add that weekly file path to `../manifest.json`.
